import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Επικοινωνία | Λογοτεχνικές Διαδρομές",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
